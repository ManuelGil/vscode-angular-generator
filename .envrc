# ============================================================
# VS Code Extension - Development Environment
# ============================================================

# Context flags
export NODE_ENV=development
export VSCODE_EXTENSION=true

# Optional: extension metadata (useful for scripts/tools)
export EXTENSION_MODE=development

# Do NOT load Node, package managers, or modify PATH here
# Tooling decisions belong to the developer environment
