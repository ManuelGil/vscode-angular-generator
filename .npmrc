# ------------------------------------------------------
# pnpm repository defaults
# ------------------------------------------------------
# These settings are applied only in repositories
# that have been validated to work correctly with
# pnpm's stricter dependency model.
# ------------------------------------------------------

node-linker=isolated
shamefully-hoist=false
